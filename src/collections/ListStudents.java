package collections;

import java.util.ArrayList;
import java.util.Collections;
import java.util.Comparator;
import java.util.HashSet;
import java.util.List;

public class ListStudents {

	public static void main(String[] args) {
		/*
		 * Student s1 = new Student(); s1.setName("Varsha"); s1.setRollNo(1); Student s2
		 * = new Student(); s2.setName("Tharunya"); s2.setRollNo(2); List<Student>
		 * students = new ArrayList<>(); students.add(s1); students.add(s2);
		 * System.out.println(students); for (Student s : students) {
		 * System.out.println(s.getName()); }
		 * students.sort(Comparator.comparing(Student::getName).reversed().thenComparing
		 * (Student::getRollNo)); for (Student s : students) { System.out.println(s); }
		 * Collections.sort(students, Comparator.comparing(Student::getName));
		 * HashSet<Integer> hs = new HashSet<>(); hs.add(1); hs.add(2); hs.add(3);
		 * hs.add(4); hs.add(5); if (hs.contains(4)) { System.out.println(true); } else
		 * { System.out.println(false); }
		 * 
		 */}

}
